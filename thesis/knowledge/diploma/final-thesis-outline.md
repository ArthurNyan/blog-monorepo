# Final Thesis Outline

## Назначение

Этот документ фиксирует финальную рабочую структуру диплома по теме:

`Разработка системы управления контентом в сфере маркетинга на базе Strapi и Astro`

Документ используется как основа для написания `content/01-introduction.tex`, `content/02-chapter-1.tex`, `content/03-chapter-2.tex` и `content/04-conclusion.tex`.

Граница финального результата дополнительно заморожена в
[final-scope.md](/Users/arthur/Documents/projects/Диплом/app-monorepo/thesis/knowledge/diploma/final-scope.md)
и должна учитываться как приоритетная по отношению к более широким ранним формулировкам.

## Общая логика диплома

Работа строится как прикладная инженерная ВКР по схеме:

1. Показать, почему маркетинговому сайту нужна современная CMS-first архитектура.
2. Обосновать выбор `headless CMS` подхода и связки `Strapi + Astro`.
3. Показать, как в рамках дипломного проекта проектируется, реализуется и доводится до результата система управления контентом.
4. Подтвердить результат архитектурой, моделями данных, сценариями публикации, деплоем и тестированием.

Ключевой принцип: диплом не должен превращаться в абстрактную работу “про сайт”. Он должен последовательно доказывать, что разработана именно система управления контентом для маркетингового сайта, где `Strapi` является ядром контентного управления, а `Astro` — публичной витриной.

Дополнительный publish-ready принцип: текст должен читаться как готовый вариант для
публикации, с которым читатель знакомится впервые. Поэтому в основной текст не должны
попадать ссылки на старые версии диплома, историю переписывания, внутренние правки и
другие следы рабочего процесса. Если требуется краткий проектный контекст, он подается
как контекст авторской разработки или архитектурные предпосылки решения, а не как
самостоятельный раздел об “исходном состоянии проекта”.

## Введение

### Цель блока

Сформулировать проблему, предметную область, цель и задачи исследования так, чтобы читатель сразу понял:

- зачем нужна такая система;
- почему тема актуальна;
- в чем инженерная новизна и практическая значимость;
- какой именно результат будет получен.

Текст введения должен быть самодостаточным для первого чтения: без пояснений о том,
какие версии формулировок были раньше и как именно раздел переписывался.

### Что должно быть в тексте

- Актуальность темы.
- Объект исследования.
- Предмет исследования.
- Цель работы.
- Задачи работы.
- Методы исследования и разработки.
- Теоретическая значимость.
- Практическая значимость.
- Краткое описание результата.

### Ключевые тезисы

- Современные маркетинговые сайты требуют частого обновления контента, мультиязычности, `SEO`-управления и быстрой публикации без участия разработчика.
- Монолитный подход к сайту затрудняет масштабирование контента и редакторскую работу.
- `Headless CMS` позволяет отделить управление контентом от публичной витрины и повысить гибкость публикации.
- Для контентных страниц маркетинговой витрины важна возможность предварительной сборки frontend-слоя, что повышает предсказуемость отдачи контента и снижает зависимость публичного сайта от runtime-логики CMS.
- Для предметной области корпоративного маркетингового сайта оправдана система, в которой редактор может управлять страницами, статьями, кейсами и вакансиями через CMS.
- Практический результат работы заключается не только в описании архитектуры, но и в разработке и доведении проектной реализации до CMS-first модели.

### Рабочая формулировка цели

Разработать систему управления контентом для корпоративного маркетингового сайта на базе `Strapi` и `Astro`, обеспечивающую управление страницами, статьями, кейсами и вакансиями, поддержку мультиязычности, предварительного просмотра, `SEO`-настроек и публикации без участия разработчика.

### Рабочий набор задач

1. Проанализировать особенности маркетинговых сайтов и подходы к управлению контентом, а также обосновать выбор `CMS-first` архитектуры на базе `Strapi` и `Astro`.
2. Сформировать функциональные и нефункциональные требования к системе, затем спроектировать ее архитектуру и модель данных.
3. Реализовать `CMS-first` контур управления страницами и основным контентом, включая `pages`, `Dynamic Zone` и вынос ключевых интерфейсных текстов в CMS.
4. Реализовать ключевые возможности публичной витрины: `ru/en`, `preview mode`, `SEO`, `Open Graph` и `sitemap`.
5. Организовать эксплуатационный контур системы, включая `roles/permissions`, защиту публичных сценариев, `webhook -> rebuild`, frontend и CMS как отдельные `Docker`-приложения в `Dokploy`.
6. Провести тестирование системы и оценить полученный результат по функциональным, локализационным, `SEO`- и эксплуатационным характеристикам.

## Глава 1. Аналитическая

### Главная цель главы

Доказать, что выбранная архитектура и стек не случайны, а логически следуют из:

- особенностей маркетинговых сайтов;
- ограничений традиционных подходов;
- требований к редакторскому процессу;
- архитектурных предпосылок авторской CMS-first разработки.

### 1.1. Особенности предметной области: маркетинговые сайты как объект автоматизации

#### Цель подраздела

Показать, что маркетинговый сайт — это не просто набор статических страниц, а контентная система с регулярными публикациями, меняющимися разделами и требованиями к управляемости.

#### Что раскрыть

- роль маркетингового сайта в цифровых коммуникациях компании;
- типичные сущности: страницы, статьи, кейсы, вакансии, формы;
- частое обновление контента как бизнес-требование;
- требования к удобству работы редактора;
- требования к поисковой видимости и локализации.

#### Ключевые тезисы

- Маркетинговый сайт должен быстро адаптироваться под новые кампании и контентные задачи.
- Значимая часть ценности сайта определяется не только визуальным представлением, но и скоростью и качеством публикации.
- Если управление контентом зависит от разработчика, это снижает эффективность маркетинговой команды.

### 1.2. Подходы к управлению контентом: традиционные CMS и headless CMS

#### Цель подраздела

Показать различие между монолитным и `headless` подходом и объяснить, почему для рассматриваемой предметной области второй лучше.

#### Что раскрыть

- базовая модель традиционной CMS;
- ограничения монолитной архитектуры;
- сущность `headless CMS`;
- API-first модель доставки контента;
- различие между runtime-рендерингом, тесно связанным с CMS, и сценарием предварительной сборки публичной витрины;
- преимущества `headless` в задачах мультиязычности, омниканальности, повторного использования контента и независимости фронтенда.

#### Ключевые тезисы

- Традиционные CMS удобны для типовых сайтов, но хуже подходят для гибкой интеграции, независимого фронтенда и сложных контентных моделей.
- `Headless CMS` позволяет развести слой контента и слой отображения.
- Для маркетинговой витрины, состоящей преимущественно из контентных страниц, предварительная сборка frontend-слоя является важным архитектурным преимуществом, поскольку позволяет отдавать уже подготовленное представление страницы.
- Для маркетингового сайта с page builder-сценарием, локализацией и гибким frontend-слоем `headless` подход архитектурно оправдан.

### 1.3. Обоснование выбора Strapi и Astro

#### Цель подраздела

Показать, почему именно `Strapi` и `Astro` являются рациональным выбором для реализации задачи.

#### Что раскрыть

- `Strapi` как headless CMS: content types, media, API, i18n, roles/permissions;
- `Astro` как frontend-витрина: статическая генерация, производительность, удобный компонентный подход;
- почему связка `Strapi + Astro` соответствует целям проекта;
- роль `Nx + pnpm` monorepo и типизированного API-контракта.

#### Ключевые тезисы

- `Strapi` дает готовую CMS-инфраструктуру без необходимости разрабатывать админ-панель с нуля.
- `Astro` подходит для маркетинговой витрины благодаря возможности предварительной сборки контентных страниц, предсказуемой отдаче результата и удобному управлению контентно-ориентированными страницами.
- Monorepo упрощает совместное развитие CMS и фронтенда.

#### Как подавать проектный контекст, если он нужен

Если в главе 1 необходимо кратко напомнить прикладной контекст проекта, его следует
встраивать в связки между подразделами или во вводные фразы уровня:

- `В рамках ВКР разрабатывается собственная CMS-first система управления контентом для корпоративной маркетинговой витрины.`
- `Архитектурная схема решения изначально ориентирована на связку Strapi как CMS-ядра и Astro как публичной витрины.`

Такой контекст допускается в одном-двух предложениях и не оформляется как самостоятельный
подраздел про baseline или “исходное состояние проекта”.

### 1.4. Формирование требований к системе

#### Цель подраздела

Сформировать формальный набор требований, который станет входом для проектной главы.

#### Что раскрыть

- функциональные требования;
- нефункциональные требования;
- редакторские сценарии;
- ограничения и нецели.
- при необходимости краткие архитектурные предпосылки решения, но без превращения этого блока в рассказ о прошлой версии проекта.

#### Функциональные требования

- управление страницами, статьями, кейсами, вакансиями;
- создание страниц через `Dynamic Zone`;
- управление `SEO`-полями и `Open Graph`;
- мультиязычность `ru/en`;
- `preview mode`;
- публикация через `webhook -> rebuild`;
- управление глобальными данными сайта;
- роли и права;
- формы и сценарии отклика.

#### Нефункциональные требования

- малая/средняя корпоративная нагрузка;
- production-базовый уровень безопасности;
- воспроизводимый deployment pipeline;
- сопровождение без изменения кода для типовых редакторских задач;
- приемлемая производительность и индексируемость страниц.

#### Нецели

- `e-commerce`;
- личные кабинеты;
- сложные workflow согласования;
- real-time совместное редактирование.

### Итог главы 1

В конце главы должно быть явно сформулировано:

- почему выбран `headless CMS` подход;
- почему выбраны `Strapi` и `Astro`;
- какие архитектурные ограничения и нецели приняты для итоговой системы;
- какие именно требования должны быть закрыты в итоговой системе.

## Глава 2. Проектная и практическая

### Главная цель главы

Показать, как именно проектируется, реализуется, разворачивается и проверяется система управления контентом на базе выбранного стека.

### 2.1. Общая архитектура системы

#### Цель подраздела

Показать место всех компонентов системы и их взаимодействие.

#### Что раскрыть

- структура monorepo;
- `apps/cms` и `apps/front`;
- роль `Strapi`, `Astro`, OpenAPI-контракта;
- общий сценарий публикации;
- место `Dokploy` и `Docker` в deployment-схеме.

#### Ключевые тезисы

- Архитектура строится вокруг разделения CMS и витрины.
- Контент управляется в `Strapi`, а публичные страницы формируются во frontend-слое.
- Для контентных страниц базовым сценарием является предварительная сборка витрины, а не формирование представления при каждом пользовательском запросе.
- Публикация запускает обновление сайта через rebuild-процесс.

### 2.2. Проектирование модели данных CMS

#### Цель подраздела

Показать, как предметная область переводится в сущности, связи и атрибуты в CMS.

#### Что раскрыть

- коллекции `pages`, `articles`, `projects`, `vacancies`;
- дополнительные сущности, например `author`, `industry`, `job-role`, `global`, `home-page`;
- `SEO`-модель;
- локализуемые поля;
- связи между сущностями;
- единые принципы именования и маршрутизации.

#### Ключевые тезисы

- Модель данных должна быть ориентирована не на шаблон сайта, а на контентную предметную область.
- `pages` — это самостоятельная сущность, а не просто набор фронтенд-маршрутов.
- `SEO` и локализация должны быть встроены в модель данных, а не добавляться поверх нее.

### 2.3. Конструктор страниц на основе Dynamic Zone

#### Цель подраздела

Показать, как система решает задачу самостоятельной сборки маркетинговых страниц редактором.

#### Что раскрыть

- почему выбран `Dynamic Zone`;
- как устроен page builder;
- какие блоки входят в систему;
- как блоки связаны с frontend-компонентами.

#### Обязательный набор блоков

- `Hero`
- `rich text`
- `features/cards`
- `CTA`
- `preview list` (`articles` / `projects` / `vacancies`)
- `logo cloud`
- `testimonials`
- `stats`
- `feature highlight`
- `FAQ`
- `process timeline`
- `quote`
- `checklist`
- `content columns`
- `numbered points`
- `lead form`

#### Ключевые тезисы

- Page builder — это ключевой практический результат диплома.
- Блоки должны быть переиспользуемыми, а не одноразовыми.
- Источники UI-референсов (`shadcn/ui`, `21st.dev`, `shadcnblocks`, `shadcn.io/blocks`) допустимы, но только после адаптации под систему и модель данных.

### 2.4. Реализация frontend-части на Astro

#### Цель подраздела

Показать, как frontend получает контент и превращает его в публичные страницы.

#### Что раскрыть

- получение данных из `Strapi`;
- рендеринг блоков;
- маршрутизация для `pages`;
- работа со страницами статей, кейсов, вакансий;
- обработка локалей;
- генерация метаданных.

#### Ключевые тезисы

- `Astro` используется не как “просто шаблонизатор”, а как производительный слой рендеринга контента.
- Frontend должен быть связан с CMS через предсказуемый контракт.
- Витрина должна корректно отображать как структурированные страницы, так и редакционный контент.

### 2.5. Мультиязычность

#### Цель подраздела

Показать, как в системе организована поддержка `ru/en`.

#### Что раскрыть

- локализация контента в `Strapi`;
- локализация основных UI-текстов;
- локализованные slug и маршруты;
- правила отображения и переключения локалей;
- возможные ограничения и fallback-стратегии.

#### Ключевые тезисы

- Мультиязычность должна быть частью архитектуры, а не набором точечных переводов.
- В финальном scope обязательный public route-контур `ru/en` ограничен storefront-core:
  `global`, `home-page`, `page`.
- Для `articles`, `projects` и `vacancies` допустимо сохранить локализуемую CMS-модель и
  локализованный preview без обязательного переноса production URLs на `/:locale/...`.
- Проверка локализации — обязательная часть результата.

### 2.6. Preview, публикация и deployment pipeline

#### Цель подраздела

Показать, как контент проходит путь от редактирования до публикации на сайте.

#### Что раскрыть

- `preview mode`;
- различие между draft и published состояниями;
- `webhook -> rebuild`;
- deployment через `Dokploy`;
- запуск и разворачивание CMS в `Docker`;
- переменные окружения и базовые production-настройки.

#### Ключевые тезисы

- `Preview` является важной частью редакторского сценария.
- Публикация должна быть воспроизводимой и безопасной.
- Deployment pipeline — не вспомогательная деталь, а часть архитектуры системы управления контентом.

#### Уточнение по альтернативам

`Strapi Cloud` может быть упомянут в дипломе как возможный managed-hosting вариант для `Strapi`, но не как основной выбранный способ развертывания текущего проекта.

### 2.7. Безопасность и роли доступа

#### Цель подраздела

Показать, как обеспечивается безопасная работа редакторов и публичных сценариев.

#### Что раскрыть

- роли и права в `Strapi`;
- доступ к `preview`;
- защита webhooks;
- защита форм и антиспам;
- базовая валидация данных.

#### Ключевые тезисы

- Для CMS-first решения безопасность нельзя сводить только к авторизации в админке.
- Защищены должны быть также публикационные и preview-сценарии.
- Роли должны соответствовать реальным редакторским функциям.

### 2.8. Тестирование и результаты

#### Цель подраздела

Подтвердить, что система действительно решает поставленные задачи.

#### Что раскрыть

- функциональные сценарии;
- проверка `ru/en` для storefront-core;
- проверка `preview`;
- проверка `SEO` и `sitemap`;
- проверка `accessibility`;
- базовая производительность;
- проверка сценария публикации без участия разработчика.

#### Рекомендуемые сценарии проверки

1. Создание новой страницы в CMS.
2. Заполнение блоков и `SEO`-данных.
3. Создание английской версии страницы.
4. Проверка предварительного просмотра.
5. Публикация страницы.
6. Проверка обновления фронтенда после rebuild.
7. Проверка корректности метаданных и `sitemap`.
8. Проверка отображения страницы на обеих локалях.
9. Проверка `accessibility` и `SEO`-метрик для `/ru/` и одной CMS-страницы.

#### Ключевые тезисы

- Результат должен быть подтвержден не только наличием кода, но и сценариями проверки.
- Важен не только рендеринг страницы, но и редакторский путь публикации.
- CMS-managed `SEO` в финальном тексте нужно трактовать как результат для `home-page`,
  `page` и detail entities `article/project/vacancy`, при отдельной фиксации того, что
  section list pages остаются route-owned.
- Тестирование должно подтверждать, что система пригодна для практического использования.

### Итог главы 2

В конце главы должно быть ясно показано:

- как устроена система;
- какие решения были приняты и почему;
- какие функции реализованы;
- как подтверждена работоспособность результата.

## Заключение

### Цель блока

Сжато показать, что поставленная цель достигнута и какие именно результаты получены.

### Что должно быть в тексте

- краткое напоминание цели;
- какие задачи решены;
- что получено в результате;
- чем полезен `Strapi + Astro` подход для предметной области;
- какие ограничения остаются;
- направления дальнейшего развития.

### Ключевые тезисы

- Разработана система управления контентом для маркетингового сайта, а не просто отдельный frontend.
- CMS-first подход позволил передать управление контентом редактору.
- Реализованные `pages`, `Dynamic Zone`, `ru/en`, `preview`, `SEO`, `sitemap` и deployment pipeline формируют целостную систему.
- Работа может быть расширена в сторону более сложных workflow, аналитики или новых типов блоков, но эти направления осознанно оставлены за рамками текущей ВКР.

## Приложения

### Цель блока

Вынести вспомогательный материал, который важен для полноты диплома, но перегружает основной текст.

### Что рекомендуется вынести в приложения

- диаграммы архитектуры;
- UML или схемы сценариев;
- таблицу контентных сущностей;
- таблицу блоков `Dynamic Zone`;
- тестовые сценарии;
- фрагменты конфигурации `SEO`, `preview`, `webhooks`;
- дополнительные иллюстрации интерфейса.

## Что особенно важно удержать по всему диплому

- Постоянно показывать связь между предметной задачей, архитектурой и конкретной реализацией.
- Не смешивать реализованное, дорабатываемое и перспективное.
- Не уходить в абстрактную теорию веб-разработки, не работающую на тему.
- Поддерживать инженерный, а не рекламный тон.
- На каждом крупном блоке отвечать на вопрос: как это помогает редактору и как это подтверждает CMS-first характер системы.

## Статус заполнения каркаса по файлам

Эта карта нужна для синхронизации `content/*.tex` с фактическим состоянием проекта.

### `content/01-introduction.tex`

Можно писать уже сейчас:

- актуальность темы;
- объект, предмет, цель и задачи;
- методы исследования и разработки;
- теоретическую и практическую значимость в общей формулировке;
- структуру работы.

Дозаполняется после проектных доработок:

- финальный абзац о достигнутом результате;
- итоговая формулировка практической значимости в привязке к реально реализованным функциям.

### `content/02-chapter-1.tex`

Можно писать уже сейчас:

- особенности маркетинговых сайтов как объекта автоматизации;
- сравнение традиционных CMS и `headless CMS`;
- обоснование выбора `Strapi` и `Astro`;
- анализ текущего состояния репозитория;
- функциональные и нефункциональные требования;
- ограничения и нецели.

Уточняется по мере доработки проекта:

- подраздел о текущих ограничениях системы относительно целевой `CMS-first` модели;
- формулировки выводов главы с учетом того, какие пробелы реально закрыты к защите.

### `content/03-chapter-2.tex`

Можно писать уже сейчас:

- общую архитектуру monorepo;
- описание текущих сущностей `Strapi`;
- текущие frontend-маршруты и интеграцию с API;
- базовую часть про локализацию на уровне схем `Strapi`;
- описание существующих публичных форм и их защиты;
- план и частично фактические сценарии тестирования.

Пишется как каркас и дозаполняется после доработок:

- `pages` и `Dynamic Zone`;
- единая `SEO`-модель и `Open Graph`;
- полноценная публичная мультиязычность `ru/en`;
- `preview mode`;
- `webhook -> rebuild`;
- deployment frontend и CMS как отдельных `Docker`-приложений в `Dokploy`;
- роли и права редакторов;
- итоговые результаты тестирования, `sitemap`, `accessibility` и производительность.

### `content/04-conclusion.tex`

Можно зафиксировать уже сейчас:

- структуру итоговых выводов;
- перечень ограничений;
- направления дальнейшего развития.

Финализируется в конце работы:

- формулировка достигнутого результата;
- перечень решенных задач;
- итоговая оценка практической полезности системы.

## Рекомендуемая последовательность дальнейшей работы

### Вариант 1. Рекомендую

Переходить к написанию диплома уже сейчас.

Для этого достаточно текущего уровня определенности, потому что:

- тема и рамки уже зафиксированы;
- структура диплома определена;
- набор референсов собран;
- цели, задачи и тезисы по разделам сформулированы;
- контур практической части понятен.

### Что писать первым

1. `Введение`
2. `Глава 1`
3. каркас `Глава 2` с подразделами и тезисами
4. затем параллельно дополнять проект и наполнять проектную главу по мере реализации

### Что еще можно уточнить до старта, но это не блокирует письмо

- окончательный перечень сущностей и полей в `Strapi`;
- точный состав `SEO`-модели;
- финальный список блоков, который точно успеет войти в защиту как реализованный;
- точный набор тестовых метрик и инструментов проверки.

### Практический вывод

На текущем этапе уже выгоднее начинать писать диплом, а оставшиеся уточнения вносить адресно по мере работы над проектом и второй главой.
