Вторая глава должна описывать не только архитектурный замысел, но и фактическое состояние
проекта, различая уже реализованные подсистемы, дорабатываемые элементы и части, которые
будут финализированы ближе к защите. Текущая версия файла выполняет роль проектного
каркаса: каждый раздел содержит ориентир по содержанию и пометку готовности текста.

\section{Общая архитектура системы}

\subsection{Состав монорепозитория и взаимодействие приложений}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по фактической структуре
репозитория.

\textbf{Текстовая заглушка:} здесь необходимо описать монорепозиторий \texttt{Nx + pnpm},
в составе которого находятся \texttt{apps/cms} на \texttt{Strapi 5} и \texttt{apps/front}
на \texttt{Astro 6}. Следует раскрыть роли компонентов: \texttt{Strapi} управляет
контентом и предоставляет API, frontend получает данные и отображает их в публичной
витрине, а общий репозиторий упрощает развитие обеих частей системы и генерацию
типизированного API-контракта. Здесь же необходимо показать, что для контентных страниц
целевой схемой является предварительная сборка публичной витрины с обновлением по
событию публикации, а не формирование представления на каждый запрос пользователя.

\subsection{Целевой сценарий публикации контента}
\textbf{Статус заполнения:} подраздел можно писать частично уже сейчас, но финальная
схема должна быть дозаполнена после реализации полного контура публикации.

\textbf{Текстовая заглушка:} в подразделе требуется описать целевую цепочку:
редактирование контента в \texttt{Strapi}, сохранение draft-версии, предварительный
просмотр, публикация, вызов \texttt{webhook} и обновление публичной витрины через rebuild.
Поскольку часть звеньев этого сценария пока не подтверждена кодом и инфраструктурными
файлами, в текущем тексте следует явно разделить уже существующие элементы и
архитектурно запланированные доработки.

\section{Проектирование модели данных CMS}

\subsection{Реализованные сущности текущей версии}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по схемам \texttt{Strapi}.

\textbf{Текстовая заглушка:} необходимо перечислить и охарактеризовать текущие сущности
\texttt{article}, \texttt{project}, \texttt{vacancy}, \texttt{vacancy-application},
\texttt{author}, \texttt{industry}, \texttt{job-role}, \texttt{home-page} и
\texttt{global}, а также показать их ключевые поля и связи. Отдельно следует отметить,
что для статей, проектов, вакансий и глобальных данных уже задействованы
\texttt{draft/publish} и \texttt{i18n}.

\subsection{Расширение модели под pages, SEO и единые маршруты}
\textbf{Статус заполнения:} подраздел относится к доработке проекта и будет
дозаполняться после реализации недостающих сущностей и полей.

\textbf{Текстовая заглушка:} в окончательной редакции здесь должно быть показано, как
предметная область расширяется за счет отдельной коллекции \texttt{pages},
\texttt{Dynamic Zone}, \texttt{SEO}-структуры, \texttt{Open Graph}-данных и единых правил
маршрутизации по локалям. До момента реализации допустимо описывать только целевую схему и
критерии проектирования, не выдавая ее за готовый функционал.

\section{Конструктор страниц на основе Dynamic Zone}

\subsection{Логика page builder-подхода}
\textbf{Статус заполнения:} раздел можно писать концептуально уже сейчас, но как описание
реализованной подсистемы он дозаполняется после появления \texttt{pages} в проекте.

\textbf{Текстовая заглушка:} требуется объяснить, почему для маркетинговой платформы
выбран page builder-сценарий, при котором редактор формирует страницу из переиспользуемых
блоков, а не зависит от жестко прошитых шаблонов. Следует связать этот выбор с задачами
маркетинга: быстрый запуск новых страниц, адаптация структуры под кампанию и повторное
использование одних и тех же секций.

\subsection{Набор блоков и правила маппинга в frontend}
\textbf{Статус заполнения:} подраздел относится к доработке проекта и наполняется после
утверждения фактического состава блоков.

\textbf{Текстовая заглушка:} в подразделе нужно привести перечень обязательных блоков
\texttt{Hero}, \texttt{rich text}, \texttt{image/media}, \texttt{CTA},
\texttt{features/cards}, \texttt{FAQ}, \texttt{testimonials}, \texttt{logo cloud},
\texttt{form/embed block}, \texttt{pricing}, \texttt{stats}, \texttt{timeline},
\texttt{case preview}, \texttt{vacancy preview} и \texttt{article preview}, а также
описать схему преобразования данных \texttt{Strapi} в \texttt{Astro/React}-компоненты.
До реализации допустимо оставить здесь таблицу-заглушку или краткий текстовый список.

\section{Реализация frontend-части на Astro}

\subsection{Текущие маршруты и витринные разделы}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по коду \texttt{apps/front}.

\textbf{Текстовая заглушка:} здесь необходимо описать текущие маршруты для главной
страницы, статей, проектов и вакансий, а также кратко охарактеризовать пользовательские
сценарии поиска вакансий и отправки отклика. Следует отдельно отметить, что главная
страница в текущем состоянии собирается из локальных компонентных секций и еще не переведена
в полноценный \texttt{CMS-first} режим.

\subsection{Интеграция с API и переход к CMS-first рендерингу}
\textbf{Статус заполнения:} подраздел пишется частично уже сейчас; финальный текст
дозаполняется после внедрения страниц \texttt{pages} и унифицированного рендеринга блоков.

\textbf{Текстовая заглушка:} в подразделе требуется описать использование сгенерированного
\texttt{OpenAPI}-клиента, получение данных из \texttt{Strapi} и текущую схему рендеринга
статей, проектов и вакансий. Затем нужно показать целевой переход к модели, в которой
frontend одинаково обрабатывает структурированные контентные сущности и динамически
собираемые маркетинговые страницы.

\section{Мультиязычность}

\subsection{Поддержка локализации в модели Strapi}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по схемам \texttt{Strapi}.

\textbf{Текстовая заглушка:} здесь нужно показать, что в текущих контентных типах уже
используется плагин \texttt{i18n}: локализуются статьи, проекты, вакансии, глобальные
данные и часть связанных сущностей. Следует описать, какие поля локализуются, как это
влияет на \texttt{slug} и почему такая схема является правильной основой для дальнейшей
публичной мультиязычности.

\subsection{Незавершенные элементы публичной локализации}
\textbf{Статус заполнения:} подраздел относится к доработке и должен быть обновлен после
реализации маршрутов и UI-текстов для \texttt{ru/en}.

\textbf{Текстовая заглушка:} необходимо честно зафиксировать, что наличие локализуемых
полей в \texttt{Strapi} еще не означает завершенную мультиязычную витрину. В текущем коде
нужно проверить и описать отсутствие либо неполноту локализованных маршрутов, переключения
языка, метаданных по локалям и стратегии \texttt{fallback}. Финальная редакция раздела
должна опираться только на реально реализованное поведение frontend.

\section{Preview, публикация и deployment pipeline}

\subsection{Draft/publish и preview-сценарий}
\textbf{Статус заполнения:} раздел можно писать частично уже сейчас на уровне текущей
модели \texttt{draft/publish}, но описание полноценного \texttt{preview mode} относится к
дальнейшей доработке проекта.

\textbf{Текстовая заглушка:} в подразделе следует развести две вещи: уже подтвержденный
механизм черновиков и публикации в \texttt{Strapi} и целевой сценарий предпросмотра
неопубликованного контента во frontend. Пока в репозитории не зафиксирован отдельный
\texttt{preview}-маршрут и защищенный протокол доступа к нему, этот фрагмент должен
оставаться описанием целевой доработки, а не готового результата.

\subsection{Webhook -> rebuild, Vercel и Docker}
\textbf{Статус заполнения:} подраздел относится к доработке и должен быть заполнен после
появления подтверждающей конфигурации.

\textbf{Текстовая заглушка:} здесь должна быть описана production-схема публикации:
frontend развертывается на \texttt{Vercel}, CMS запускается в \texttt{Docker}, а публикация
контента инициирует rebuild публичной витрины через \texttt{webhook}. До появления
фактических конфигурационных файлов, переменных окружения и проверенных сценариев этот
подраздел следует сохранять как каркас с перечнем ожидаемых элементов и точек проверки.

\section{Безопасность и роли доступа}

\subsection{Публичные формы и базовая защита пользовательского ввода}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по существующей форме отклика.

\textbf{Текстовая заглушка:} необходимо описать, что модуль отклика на вакансию уже
обладает признаками production-базовой защиты: валидация формы, \texttt{honeypot},
\texttt{rate limit} и загрузка резюме через контролируемый сценарий. Раздел должен
показывать, что безопасность в проекте рассматривается не только как авторизация в админке,
но и как защита публичных точек ввода.

\subsection{Роли редакторов, защита preview и публикации}
\textbf{Статус заполнения:} подраздел будет дозаполняться после финальной настройки
ролей и публикационных сценариев.

\textbf{Текстовая заглушка:} здесь следует описать целевое распределение ролей в
\texttt{Strapi}, разграничение доступа к редактированию и публикации, а также защиту
\texttt{preview}- и \texttt{webhook}-маршрутов. Если на момент написания часть этих
настроек еще не реализована, это должно быть прямо обозначено как дорабатываемый блок.

\section{Тестирование и результаты}

\subsection{Сценарии, которые можно проверять уже сейчас}
\textbf{Статус заполнения:} подраздел можно писать уже сейчас, но только на основании
фактически существующих сценариев.

\textbf{Текстовая заглушка:} здесь следует описать проверяемые уже сегодня сценарии:
получение и отображение статей, проектов и вакансий; фильтрация вакансий; отправка отклика;
работа локализуемых полей на уровне \texttt{Strapi}; корректность \texttt{OpenAPI}-контракта.
Если будут приложены скриншоты, таблицы или трассировки запросов, они должны относиться к
актуальному состоянию проекта.

\subsection{Сценарии итоговой проверки перед защитой}
\textbf{Статус заполнения:} подраздел формируется как план проверки сейчас и будет
финализирован после завершения недостающих функций.

\textbf{Текстовая заглушка:} в окончательной редакции необходимо зафиксировать полную
матрицу проверки: создание страницы \texttt{pages}, заполнение блоков и \texttt{SEO},
подготовка английской версии, предпросмотр, публикация, rebuild фронтенда,
проверка метаданных, \texttt{sitemap}, \texttt{accessibility} и базовой производительности.
До момента реализации этих сценариев допустимо оставить здесь план тестирования и критерии
приемки.

\section{Выводы по проектной главе}
\textbf{Статус заполнения:} краткий вывод можно написать уже сейчас, а финальная редакция
должна быть обновлена после завершения практической части.

\textbf{Текстовая заглушка:} в конце главы следует свести воедино архитектуру системы,
модель данных, статус реализации контентных сущностей, состояние мультиязычности,
публикационного контура и результаты тестирования. В выводе необходимо явно развести:
что уже реализовано и подтверждено, что находится в доработке к защите, а какие элементы
сознательно оставлены в перспективе.
