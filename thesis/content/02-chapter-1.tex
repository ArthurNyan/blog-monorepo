Первая глава должна обосновать, почему для предметной области корпоративного
маркетингового сайта выбран именно \texttt{CMS-first} подход и связка
\texttt{Strapi + Astro}. В текущей версии файла зафиксирован аналитический каркас:
подразделы уже соответствуют финальному контуру диплома, а текстовые заглушки указывают,
какие фрагменты можно писать немедленно и какие места позже потребуется синхронизировать
с итоговым состоянием проекта.

\section{Особенности маркетинговых сайтов как объекта автоматизации}

\subsection{Роль маркетингового сайта в цифровых коммуникациях компании}
\textbf{Статус заполнения:} подраздел пишется уже сейчас на основе предметной области,
зафиксированной в \texttt{thesis-brief.md}.

\textbf{Текстовая заглушка:} здесь следует показать, что маркетинговый сайт выполняет не
только представительскую, но и операционную функцию: публикация страниц под кампании,
обновление статей, демонстрация проектов компании, размещение вакансий и сбор откликов.
Нужно подчеркнуть, что для такой платформы критична не только визуальная часть, но и
скорость управляемого обновления контента.

\subsection{Контентные сущности и редакторские сценарии}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} необходимо описать базовые сущности предметной области:
страницы, статьи, проекты, вакансии, отклики, а также глобальные данные сайта. Далее
следует связать эти сущности с редакторскими сценариями: создание страницы, локализация
материала, предпросмотр, публикация и поддержка \texttt{SEO}-метаданных. В финальной
редакции полезно выделить контент-менеджера как основного пользователя системы.

\section{Подходы к управлению контентом: традиционные CMS и headless CMS}

\subsection{Ограничения традиционного CMS-подхода}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} требуется описать архитектуру традиционных \texttt{CMS},
в которых администрирование, хранение данных и публичное отображение тесно связаны.
Следует показать, что такой подход удобен для типовых сайтов, но хуже соответствует
требованиям независимого frontend-слоя, гибкой локализации, повторного использования
контента и управляемого deployment-процесса.

\subsection{Преимущества headless CMS для маркетинговой платформы}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} здесь необходимо раскрыть сущность \texttt{headless CMS} как
\texttt{API-first} подхода, при котором управление контентом отделяется от слоя рендеринга.
Важно связать преимущества не с абстрактной теорией, а с задачами конкретной системы:
локализуемые сущности, переиспользуемые блоки страниц, независимое развитие frontend,
\texttt{preview mode}, публикация через \texttt{webhook -> rebuild} и возможность строгого
контракта между \texttt{CMS} и витриной.

\section{Обоснование выбора Strapi и Astro}

\subsection{Strapi как ядро управления контентом}
\textbf{Статус заполнения:} подраздел пишется уже сейчас и может опираться на текущую
структуру \texttt{apps/cms}.

\textbf{Текстовая заглушка:} в подразделе следует обосновать выбор \texttt{Strapi} как
платформы для моделирования контентных сущностей, локализации, работы с draft/publish и
предоставления \texttt{REST API}. Уже сейчас можно ссылаться на наличие в проекте сущностей
\texttt{article}, \texttt{project}, \texttt{vacancy}, \texttt{vacancy-application},
\texttt{industry}, \texttt{job-role}, \texttt{home-page} и \texttt{global}. Перед финальной
редакцией этот подраздел нужно синхронизировать с фактическим составом сущностей к защите.

\subsection{Astro как публичная витрина и monorepo как среда разработки}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} здесь необходимо показать, что \texttt{Astro} выбран как
frontend-слой для контентно-ориентированного сайта благодаря компонентной модели и
возможности эффективной генерации публичных страниц. Отдельно следует описать роль
монорепозитория \texttt{Nx + pnpm}: совместная разработка \texttt{CMS} и frontend,
централизованные команды сборки и поддержка типизированного \texttt{OpenAPI}-контракта.

\section{Анализ исходного состояния проекта и его ограничений}

\subsection{Что уже реализовано в текущем репозитории}
\textbf{Статус заполнения:} подраздел пишется уже сейчас по фактическому коду.

\textbf{Текстовая заглушка:} необходимо зафиксировать, что в репозитории уже присутствуют
два приложения: \texttt{apps/cms} на \texttt{Strapi 5} и \texttt{apps/front} на
\texttt{Astro 6}. Во frontend уже реализованы маршруты и интерфейсы для статей, проектов,
вакансий и формы отклика; во backend присутствуют соответствующие контентные типы и сидер
для вакансий. Эти факты позволяют рассматривать диплом как развитие реального проекта, а
не абстрактный концепт.

\subsection{Какие ограничения мешают считать систему полностью CMS-first}
\textbf{Статус заполнения:} подраздел можно писать уже сейчас, но он будет уточняться по
мере закрытия проектных задач.

\textbf{Текстовая заглушка:} здесь следует честно показать текущие ограничения:
в репозитории пока не выявлена отдельная сущность \texttt{pages}; главная страница
\texttt{apps/front/src/pages/index.astro} собирается из локальных компонентов и пока не
управляется через \texttt{Strapi}; в \texttt{MainLayout} отсутствует развернутая
\texttt{SEO}-обвязка и локаль в разметке задана статически; в коде не подтверждены
готовые сценарии \texttt{preview mode}, \texttt{sitemap}, \texttt{webhook -> rebuild},
\texttt{Vercel}-deployment и \texttt{Docker}-конфигурация CMS. Этот подраздел должен
оставаться фактоориентированным и обновляться только по подтвержденным изменениям проекта.

\section{Формирование требований к системе}

\subsection{Функциональные требования}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} необходимо сформулировать требования к управлению страницами,
статьями, проектами, вакансиями и откликами; к сборке маркетинговых страниц через
\texttt{Dynamic Zone}; к поддержке \texttt{ru/en}; к управлению \texttt{SEO}-полями и
\texttt{Open Graph}; к предварительному просмотру; к публикации через
\texttt{webhook -> rebuild}; к ролям и правам; к глобальным данным сайта; к корректной
работе публичных форм. При финализации подраздела требуется оставить только тот набор
функций, который соответствует целям и фактическому объему работы.

\subsection{Нефункциональные требования}
\textbf{Статус заполнения:} подраздел пишется уже сейчас.

\textbf{Текстовая заглушка:} следует зафиксировать требования к сопровождаемости,
воспроизводимости deployment-процесса, базовой безопасности, приемлемой производительности
и индексируемости страниц, а также к малой или средней корпоративной нагрузке.
Нефункциональные требования должны быть прямо связаны с выбранной архитектурой и не должны
превращаться в общий обзор требований к веб-системам.

\subsection{Ограничения и нецели проекта}
\textbf{Статус заполнения:} подраздел пишется уже сейчас и в дальнейшем меняется только
при явном пересмотре рамок диплома.

\textbf{Текстовая заглушка:} в подразделе требуется явно указать, что в объем работы не
входят \texttt{e-commerce}, личные кабинеты, сложные согласовательные процессы и
совместное редактирование в реальном времени. Одновременно необходимо подчеркнуть, что
\texttt{preview mode}, базовые редакторские сценарии, защита публикации и защита форм
напротив входят в обязательный объем ВКР.

\section{Выводы по аналитической главе}
\textbf{Статус заполнения:} короткий промежуточный вывод можно написать уже сейчас, но
финальная редакция должна быть синхронизирована с результатами проектной главы.

\textbf{Текстовая заглушка:} в завершении главы необходимо свести вместе четыре вывода:
почему маркетинговый сайт требует развитого контентного управления; почему для него
предпочтителен \texttt{headless CMS} подход; почему рационально выбраны \texttt{Strapi} и
\texttt{Astro}; какие ограничения исходного состояния должны быть устранены в практической
части. Вывод не должен дублировать введение, а должен логически переводить читателя к
архитектуре, данным, публикации и тестированию во второй главе.
