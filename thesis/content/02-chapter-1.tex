В первой главе маркетинговый сайт рассматривается не как набор отдельных страниц, а как
предметная область, в которой эксплуатационная ценность системы определяется скоростью,
управляемостью и предсказуемостью контентных изменений. Поэтому аналитика выстроена в
последовательности ``особенности предметной области -> архитектурный выбор -> требования
к системе'': сначала выделяются свойства маркетинговой витрины как объекта автоматизации,
затем оцениваются подходы к управлению контентом и после этого формируется набор
требований, который используется как основание для проектных решений второй главы. В
рамках ВКР такая логика принципиальна, поскольку разрабатывается не просто публичный
frontend, а \texttt{CMS-first} система управления контентом для корпоративной
маркетинговой платформы.

\section{Особенности маркетинговых сайтов как объекта автоматизации}

\subsection{Роль маркетингового сайта в цифровых коммуникациях компании}

Маркетинговый сайт в современной организации выполняет роль публичной цифровой витрины,
через которую компания представляет продукты и услуги, формирует профессиональную
репутацию, публикует экспертные материалы, демонстрирует выполненные проекты и поддерживает
коммуникацию с потенциальными клиентами и соискателями. В отличие от простого
информационного сайта, он должен быстро адаптироваться к новым маркетинговым кампаниям,
изменениям позиционирования, запуску новых направлений и потребности в регулярном
обновлении содержимого~\cite{torgunakova2023marketing-website,voroniuk2020retail-cms-needs}.

Однако для инженерного анализа важен не сам факт наличия таких материалов, а режим их
изменения. В маркетинговом контуре содержание сайта обновляется не эпизодически, а в
ответ на кампании, запуск новых предложений, публикацию экспертных материалов, изменения
позиционирования и кадровые сценарии. Следовательно, система должна поддерживать частые
редакторские операции как нормальный режим работы, а не как исключение. Это смещает
фокус проектирования с разовой разработки шаблонов на организацию управляемого
контентного контура, в котором типовые изменения не требуют вмешательства в код
публичной витрины.

Из этого вытекает и второй важный вывод: ценность маркетингового сайта определяется не
только качеством визуального представления, но и тем, насколько быстро и предсказуемо
он позволяет провести материал через цикл ``подготовка -> проверка -> публикация''.
Если типовые контентные изменения зависят от разработчика, возникает организационное
узкое место: маркетинговая команда не может оперативно обновлять страницы, публикации и
метаданные, а сопровождение сайта начинает смешиваться с задачами развития frontend.
Следовательно, объектом автоматизации становится не только отображение контента, но и
сам редакторский процесс, включая его роли, точки контроля и публикационный контур.

Для корпоративного маркетингового сайта также существенны требования к поисковой
видимости, локализации и управляемой публикации. Контент должен быть пригоден для
индексации, сопровождаться метаданными, поддерживать локализованные версии и
публиковаться в воспроизводимом контуре, не зависящем от ручных действий в кодовой
базе. Эти свойства делают сайт не просто набором маршрутов, а контентной системой со
своими пользовательскими ролями, моделями данных и эксплуатационными сценариями. Для
русскоязычных работ по
информационной архитектуре и корпоративным сайтам также характерен акцент на устойчивой
навигации, структурной согласованности контента и централизованном управлении
информационным слоем~\cite{belyaev2009website-navigation,mikhailova2016content-navigation,mashina2022centralized-cms-knowledge}.

В таблице~\ref{tab:marketing-domain-engineering-effects} сведены ключевые свойства
рассматриваемой предметной области и их прямые инженерные последствия для проектируемой
системы.

\Needspace{12\baselineskip}
\begin{table}[H]
  \centering
  \small
  \caption{Связь особенностей маркетингового сайта с инженерными требованиями к системе}
  \label{tab:marketing-domain-engineering-effects}
  \begin{tabular}{|L{0.34\linewidth}|L{0.56\linewidth}|}
    \hline
    \textbf{Особенность предметной области} & \textbf{Инженерное следствие для системы} \\
    \hline
    Частые изменения контента в ответ на кампании, публикации и изменения позиционирования &
    Типовые обновления должны выполняться через управляемые контентные сущности и
    редакторский интерфейс, без ручного изменения frontend-кода. \\
    \hline
    Необходимость независимого редакторского цикла &
    Система должна разделять роли редактирования и разработки, поддерживать черновой
    режим, проверку материала и публикацию как самостоятельный контур. \\
    \hline
    Мультиязычность публичного контента &
    Локализация должна проектироваться на уровне данных, маршрутов и редакторских
    сценариев, а не сводиться к разрозненным текстовым вставкам в шаблонах. \\
    \hline
    Управление \texttt{SEO} и метаданными &
    Метаданные должны быть частью контентной модели, чтобы редактор мог управлять
    индексируемостью, заголовками, описаниями и \texttt{Open Graph} без участия
    разработчика. \\
    \hline
    Необходимость предварительного просмотра до публикации &
    Требуется отдельный сценарий \texttt{preview}, позволяющий проверить будущую
    публичную версию до вывода материала в основной контур. \\
    \hline
    Требование воспроизводимой схемы публикации &
    Публикация должна рассматриваться как формализованный процесс обновления витрины, а
    не как набор ручных операций в кодовой базе и инфраструктуре. \\
    \hline
  \end{tabular}
\end{table}

\subsection{Контентные сущности и редакторские сценарии}

Для рассматриваемой предметной области характерен ограниченный, но содержательно важный
набор сущностей. К ним относятся произвольные маркетинговые страницы, статьи, проекты,
вакансии, отклики на вакансии, а также глобальные данные сайта. Каждая из этих сущностей
имеет собственный жизненный цикл и собственные требования к редактированию. Например,
маркетинговая страница должна собираться из переиспользуемых блоков, статья требует
структурированного текста, обложки и авторства, проект должен показывать портфолио
выполненных работ, а вакансия должна не только отображаться на витрине, но и быть связана
со сценарием отправки отклика.

Инженерно значимо то, что перечисленные сущности не сводятся к одному шаблону
``страница с текстом''. Они требуют разных структур данных, разных правил публикации и
разных связей между редакторским и публичным контуром. Если система хранит такие
материалы как слабо структурированный набор полей, то редакторский процесс быстро
становится зависимым от частных доработок интерфейса и ручной правки представления.
Следовательно, уже на уровне предметной области возникает требование к явной
контентной модели, в которой страницы, статьи, проекты, вакансии и глобальные данные
оформлены как самостоятельные сущности со своими атрибутами и жизненным циклом.

Отсюда вытекает основной редакторский сценарий: контент-менеджер или маркетолог должен
создать или изменить материал, заполнить структурированные поля, при необходимости
подготовить локализованную версию, проверить публичное представление и опубликовать
результат без ручного изменения frontend-кода. В этой логике \texttt{SEO}-поля,
метаданные \texttt{Open Graph}, локализованные маршруты и предварительный просмотр
перестают быть второстепенными опциями интерфейса и становятся обязательными элементами
архитектуры редакторского контура.

Таким образом, маркетинговый сайт представляет собой систему, в которой ценность создается
не только конечной HTML-страницей, но и качеством управления ее содержимым. Это отличает
рассматриваемую предметную область от проектов, где достаточно один раз сверстать
несколько шаблонов и редко возвращаться к их содержимому. Для ВКР по разработке системы
управления контентом такое различие принципиально: требуется проектировать именно
управляемую контентную платформу, а не только витринный интерфейс. Именно поэтому
следующим шагом анализа становится сравнение архитектурных подходов к управлению
контентом: необходимо определить, какая модель \texttt{CMS} лучше соответствует
требованиям независимого редакторского цикла, мультиязычности, управляемого
\texttt{SEO}, \texttt{preview} и воспроизводимой публикации.

\section{Подходы к управлению контентом: традиционные CMS и headless CMS}

\subsection{Ограничения традиционного CMS-подхода}

Традиционная \texttt{CMS} обычно объединяет в одном контуре хранение данных, административный
интерфейс, шаблоны отображения и механизм вывода страницы пользователю. Такая архитектура
хорошо подходит для типовых сайтов, в которых структура представления относительно стабильна,
а публичный слой тесно связан с самой системой управления контентом. Для небольших задач это
снижает порог входа и ускоряет старт разработки, поскольку многие возможности доступны из
коробки~\cite{yesikov2013cms-comparative-analysis}.
Подобная логика характерна и для \texttt{CMS}-ориентированных framework-подходов,
автоматизирующих создание типовых веб-сайтов и CRUD-контуров, где приоритет отдается
скорости запуска и унификации административной части~\cite{bandirmali2018mtcmf}.

Для задачи маркетингового сайта принципиально, что преимущества такого подхода относятся
прежде всего к стартовой простоте, а не к гибкости архитектуры. Публичная витрина в этом
случае зависит от шаблонного и runtime-слоя самой \texttt{CMS}, поэтому развитие frontend
обычно происходит внутри ограничений конкретной темы, движка и способа рендеринга. Это
усложняет ситуацию, когда требуется независимо изменять структуру маркетинговых страниц,
переиспользовать одни и те же контентные сущности в разных разделах или выносить часть
публичного слоя в отдельный технологический контур.

Интеграция редактирования и рендеринга действительно упрощает базовый \texttt{preview} и
публикацию: редактор работает в той же системе, которая формирует страницу. Однако для
рассматриваемой предметной области этого недостаточно. Если локализация, \texttt{SEO},
метаданные и публикационный процесс завязаны на тему и внутренний runtime \texttt{CMS},
то они начинают зависеть не столько от контентной модели, сколько от частных доработок
слоя представления. В результате традиционный подход не исключает нужные функции, но
делает их вторичными по отношению к связанному шаблонному контуру, тогда как в ВКР
требуется архитектура, где редакторский и публичный слои могут развиваться раздельно.

\subsection{Преимущества headless CMS для маркетинговой платформы}

\texttt{Headless CMS} исходит из другой модели: система управления отвечает за хранение
и редактирование структурированных данных, а отображение контента переносится в отдельный
frontend-слой. Такой подход естественным образом соответствует \texttt{API-first} логике,
в которой \texttt{CMS} предоставляет данные через программный интерфейс, а витрина или
другой потребитель решает, как именно эти данные представить пользователю. Для
\texttt{CMS}-ориентированной разработки это означает более явное разделение контентной
модели, правил публикации и публичного интерфейса, что хорошо согласуется с современными
model-driven и platform-independent подходами в домене \texttt{CMS}~\cite{priefer2021cms-mdd-domain,gkantouna2019cms-oriented-modeling,filipe2016xis-cms}.

Для маркетингового сайта эта модель важна не сама по себе, а как способ перевести
ключевые требования предметной области в архитектурные свойства системы. Когда frontend
не встроен в \texttt{CMS}, контентная модель может проектироваться относительно сущностей
и редакторских сценариев, а не относительно одного набора шаблонов. Это упрощает
переиспользование контента, позволяет строить локализованную маршрутизацию и дает основу
для отдельного публикационного контура, в котором обновление данных не требует ручной
правки публичной витрины~\cite{santahuhta2022headless-cms,matjuhhin2026api-driven-cms,holcik2025headless-cloud}.

При этом \texttt{headless} подход не снимает инженерные задачи автоматически. В частности,
\texttt{preview}, управление метаданными и публикация должны проектироваться явно, так как
\texttt{CMS} не рендерит итоговую страницу самостоятельно. Это усложняет начальную
интеграцию, но одновременно делает публикационный контур явным и проверяемым: редакторский
слой, \texttt{API}, frontend-сборка и публичная выдача перестают смешиваться в одном
runtime-сценарии. Для мультиязычных контентных платформ это особенно существенно, поскольку
локаль влияет не только на тексты интерфейса, но и на маршруты, метаданные и доступность
данных в разных языковых версиях~\cite{santahuhta2022headless-cms,oleary2008multilingual-kms,gracia2012multilingual-web-data,solovev2023multilingual-web-app}.

Для формализации выбора архитектурного подхода под задачу маркетинговой витрины
целесообразно сопоставить оба варианта по критериям, которые прямо следуют из предметной
области и целевого результата ВКР (таблица~\ref{tab:cms-vs-headless-marketing}).

\Needspace{12\baselineskip}
\begin{table}[H]
  \centering
  \footnotesize
  \caption{Сравнение традиционной CMS и headless CMS по критериям маркетингового сайта}
  \label{tab:cms-vs-headless-marketing}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{|L{0.16\linewidth}|L{0.20\linewidth}|L{0.20\linewidth}|L{0.26\linewidth}|}
    \hline
    \textbf{Критерий} & \textbf{Традиционная CMS} & \textbf{Headless CMS} & \textbf{Значение для задачи ВКР} \\
    \hline
    Зависимость frontend-слоя от \texttt{CMS} &
    Шаблоны и рендеринг обычно встроены в \texttt{CMS}; развитие витрины зависит от ее theme- и runtime-слоя. &
    Публичный frontend развивается отдельно и использует \texttt{CMS} как источник данных по \texttt{API}. &
    Для независимого развития витрины и редакторского контура предпочтительно слабое связывание представления с \texttt{CMS}. \\
    \hline
    Пригодность к мультиязычности &
    Локализация часто расширяется на уровне шаблонов, плагинов и отдельных страниц. &
    Локаль можно проектировать на уровне данных, \texttt{API}, маршрутов и метаданных как единый контур. &
    Для обязательной схемы \texttt{ru/en} требуется архитектурная, а не точечная локализация. \\
    \hline
    Повторное использование контента &
    Контент нередко привязан к конкретным шаблонам и страницам сайта. &
    Структурированные сущности и блоки проще использовать повторно в разных страницах и каналах. &
    Маркетинговые страницы, статьи, проекты и вакансии требуют общей контентной модели, а не набора изолированных шаблонов. \\
    \hline
    \texttt{Preview} и управляемая публикация &
    Базовый \texttt{preview} проще, так как редактирование и рендеринг находятся в одном контуре; публикация тесно связана с runtime \texttt{CMS}. &
    \texttt{Preview} требует отдельной интеграции с витриной, но публикацию можно оформить как явный pipeline между \texttt{CMS} и публичным слоем. &
    Для диплома важен не только просмотр черновика, но и формализованный путь ``черновик -> проверка -> публичная витрина''. \\
    \hline
    Управляемость \texttt{SEO} и метаданных &
    Базовые настройки часто доступны из коробки, но их логика сильнее связана с темой и шаблонами. &
    \texttt{SEO}-поля и метаданные нужно моделировать явно, после чего ими можно управлять как частью контентной сущности. &
    Для маркетингового сайта критично редакторски управляемое \texttt{SEO}: \texttt{canonical}, \texttt{meta} и \texttt{Open Graph} без правки frontend-кода. \\
    \hline
    Удобство для воспроизводимого deployment-контура &
    Стартовый deploy проще как одного приложения, но публичная выдача и администрирование остаются в одном runtime-контуре. &
    Контуры \texttt{CMS} и витрины разводятся, что требует интеграции, но упрощает versioned deployment и изоляцию публикации. &
    Для rebuild-based публикации важнее предсказуемый и воспроизводимый pipeline, чем минимальное число исполняемых компонентов. \\
    \hline
  \end{tabular}
\end{table}

Матрица показывает, что традиционный подход сохраняет преимущество в стартовой простоте
и во встроенности базового \texttt{preview}, однако по критериям, определяющим предметную
область данной ВКР, решающими оказываются другие свойства: независимость frontend-слоя,
сквозная мультиязычность, переиспользование контента, редакторски управляемое \texttt{SEO} и
воспроизводимая публикация. Эти требования могут быть реализованы и поверх традиционной
\texttt{CMS}, но для нее они обычно выступают как надстройки над связанным шаблонным
контуром. Для \texttt{headless CMS} те же свойства являются прямым следствием разделения
контентного и презентационного слоев. Следовательно, выбор \texttt{headless CMS} в рамках
данной работы является не данью технологическому тренду, а рациональным ответом на
критерии, заданные архитектурой корпоративного маркетингового сайта.

\section{Обоснование выбора Strapi и Astro}

\subsection{Strapi как ядро управления контентом}

После выбора \texttt{headless}-подхода необходимо обосновать уже не только сам класс
архитектуры, но и конкретную технологическую связку. Для рассматриваемой ВКР выбор
\texttt{Strapi}, \texttt{Astro} и монорепозитория \texttt{Nx + pnpm} определяется не
полнотой их возможностей как таковой, а тем, насколько они закрывают критерии,
непосредственно вытекающие из предметной области маркетингового сайта
(таблица~\ref{tab:stack-selection-criteria}).

\Needspace{12\baselineskip}
\begin{table}[H]
  \centering
  \footnotesize
  \caption{Критерии выбора стека для \texttt{CMS-first} маркетинговой платформы}
  \label{tab:stack-selection-criteria}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{|L{0.24\linewidth}|L{0.28\linewidth}|L{0.38\linewidth}|}
    \hline
    \textbf{Критерий выбора} & \textbf{Почему он важен} & \textbf{Как его закрывает выбранный стек} \\
    \hline
    Готовая \texttt{CMS}-инфраструктура &
    Цель ВКР состоит в разработке системы управления контентом для маркетинговой команды, а
    не в создании административной панели и базовых редакторских механизмов с нуля. &
    \texttt{Strapi} предоставляет административный интерфейс, контентные \texttt{API},
    \texttt{Draft \& Publish}, \texttt{Preview}, media-layer и базовую ролевую модель, что
    позволяет сосредоточить разработку на предметной модели и публикационном контуре~\cite{strapi-cms-docs}. \\
    \hline
    Структурированные content types, \texttt{i18n} и \texttt{roles/permissions} &
    Страницы, статьи, проекты и вакансии имеют разный жизненный цикл, а локализация
    \texttt{ru/en} и разделение ролей редактора, маркетолога и администратора должны быть
    частью архитектуры данных, а не набором частных доработок шаблонов. &
    \texttt{Strapi} позволяет моделировать самостоятельные контентные сущности,
    локализованные записи и разграничение доступа на уровне \texttt{CMS}, то есть там, где
    фактически происходит редакторская работа~\cite{strapi-cms-docs}. \\
    \hline
    Предсобранный frontend-контур для маркетинговой витрины &
    Для контентно-ориентированных страниц важны предсказуемая отдача, индексируемость и
    минимальная зависимость публичной витрины от runtime-логики \texttt{CMS}. &
    \texttt{Astro} ориентирован на предварительную сборку маршрутов и страниц, поэтому
    основной публичный слой может публиковаться как заранее подготовленная витрина, а
    рендеринг по запросу использоваться только там, где он действительно необходим~\cite{astro-on-demand}. \\
    \hline
    Формализованный сценарий публикации &
    При разделении \texttt{CMS} и витрины публикация должна быть воспроизводимым процессом,
    а не ручной синхронизацией данных и frontend-кода. &
    В связке \texttt{Strapi + Astro} изменение контента логически переводится в схему
    \texttt{webhook -> rebuild}: \texttt{CMS} фиксирует публикационное событие, а frontend
    пересобирает публичную витрину на основе обновленных данных~\cite{astro-strapi-guide}. \\
    \hline
    Согласованное развитие \texttt{CMS} и frontend &
    Контентная схема, \texttt{API}-контракт и рендеринг блоков изменяются совместно;
    раздельное сопровождение повышает риск рассинхронизации между двумя частями системы. &
    Монорепозиторий \texttt{Nx + pnpm} позволяет хранить оба приложения в одном workspace,
    выполнять атомарные изменения, использовать единый набор зависимостей и согласованные
    команды сборки, что особенно важно для общего контентного контракта~\cite{nx-monorepos}. \\
    \hline
  \end{tabular}
\end{table}

Решающим аргументом в пользу \texttt{Strapi} является не абстрактная популярность
платформы, а то, что она закрывает сразу два базовых критерия: наличие готовой
\texttt{CMS}-инфраструктуры и возможность строить управление на основе структурированной
контентной модели. Согласно официальной документации \texttt{Strapi 5}, система
поддерживает контентные типы, административное управление содержимым,
\texttt{Draft \& Publish}, международную локализацию, \texttt{Preview},
\texttt{Role-Based Access Control} и работу через контентные \texttt{API}~\cite{strapi-cms-docs}.
Для темы данной ВКР это принципиально, поскольку позволяет сосредоточить инженерные
усилия на моделировании страниц, статей, проектов, вакансий и связанных редакторских
сценариев, а не на разработке базовой \texttt{CMS}-обвязки.

Не менее важно, что \texttt{Strapi} работает со структурированными сущностями, а не с
жестко зашитым набором шаблонов. Для маркетингового сайта это означает возможность
развести в модели данных самостоятельные типы контента, локализованные версии записей и
ролевое разграничение доступа. В результате требования к \texttt{i18n},
\texttt{roles/permissions} и управляемому редакторскому циклу закрепляются на уровне
\texttt{CMS}-ядра, то есть в том слое, где действительно создается и публикуется
контент, а не в виде поздних надстроек над frontend-частью.

\subsection{Astro как публичная витрина и \texttt{Nx + pnpm} как среда согласованного развития}

\texttt{Astro} выбран не просто как еще один frontend-framework, а как средство закрыть
критерий предсобранного публичного контура. Официальная документация указывает, что по
умолчанию страницы и маршруты в \texttt{Astro} предварительно рендерятся во время сборки,
а переход к рендерингу по запросу выполняется только для специально выделенных
сценариев~\cite{astro-on-demand}. Для маркетинговой витрины это соответствует характеру
нагрузки: основной массив страниц является контентным и должен отдаваться как уже
подготовленное представление, пригодное для индексирования и не зависящее от шаблонного
runtime самой \texttt{CMS}.

Из этого архитектурного решения непосредственно вытекает и схема
\texttt{webhook -> rebuild}. Если \texttt{Strapi} отвечает за хранение и публикацию
контента, а \texttt{Astro} --- за выпуск предсобранной витрины, то обновление
публичного слоя не может сводиться к мгновенному рендерингу внутри \texttt{CMS}. Оно
должно оформляться как явный публикационный переход от события в \texttt{CMS} к
пересборке frontend-слоя. Именно поэтому для статического сценария совместного
использования \texttt{Strapi} и \texttt{Astro} в официальном руководстве также
предполагается пересборка по \texttt{webhook}~\cite{astro-strapi-guide}. В контексте
данной ВКР это не частная техническая деталь, а логическое следствие выбранной
\texttt{CMS-first} архитектуры.

Монорепозиторий \texttt{Nx + pnpm} дополняет эту связку на уровне сопровождения.
Официальная документация \texttt{Nx} подчеркивает преимущества общего репозитория для
нескольких приложений, включая атомарные изменения и единый набор инженерных
процедур~\cite{nx-monorepos}. Для рассматриваемого проекта это важно не само по себе, а
потому, что \texttt{CMS} и витрина развиваются как две части одной системы: изменение
контентной схемы влияет на \texttt{API}-контракт, а изменение контракта влияет на
frontend-рендеринг и публикационный процесс. Следовательно, \texttt{Nx + pnpm} полезен
именно как средство согласованного развития \texttt{Strapi}- и \texttt{Astro}-контуров в
одной кодовой базе, с меньшим риском рассинхронизации между моделью данных и публичной
витриной.

Таким образом, выбор \texttt{Strapi}, \texttt{Astro} и \texttt{Nx + pnpm} обоснован через
связанный набор критериев: готовую \texttt{CMS}-инфраструктуру, структурированную
контентную модель, предсобранный frontend-контур, воспроизводимую публикацию и
согласованное сопровождение двух приложений. На этой основе далее можно формировать
требования к разрабатываемой системе, не смешивая аналитическую аргументацию с деталями
проектной реализации второй главы.

\section{Формирование требований к системе}

\subsection{Функциональные требования}

Для аналитической главы недостаточно просто перечислить требуемые возможности. Чтобы
требования действительно выступали входом в проектирование, каждое из них должно быть
связано с источником возникновения, целевым контуром реализации и способом последующей
проверки. В рассматриваемой ВКР функциональные требования распределяются между двумя
связанными пользовательскими контурами: редакторским, в котором контент создается,
проверяется и публикуется, и публичным, в котором результат должен отдаваться как
управляемая и предсказуемая маркетинговая витрина.

\Needspace{12\baselineskip}
\begingroup
\footnotesize
\begin{longtable}{|L{0.22\linewidth}|L{0.22\linewidth}|L{0.46\linewidth}|}
  \caption{Трассировка функциональных требований к системе}
  \label{tab:functional-requirements-traceability} \\
  \hline
  \textbf{Требование} & \textbf{Источник требования} & \textbf{Контур реализации и верификации} \\
  \hline
  \endfirsthead
  \caption*{Продолжение таблицы~\ref{tab:functional-requirements-traceability}} \\
  \hline
  \textbf{Требование} & \textbf{Источник требования} & \textbf{Контур реализации и верификации} \\
  \hline
  \endhead
  \hline
  \endfoot
  Управление ключевыми контентными сущностями и глобальными данными сайта &
  Частые контентные изменения, различие жизненных циклов страниц, статей, проектов,
  вакансий и общих данных витрины &
  Реализация раскрывается в главе~2 через общую архитектуру, модель данных \texttt{CMS} и
  \texttt{CMS}-управляемые глобальные данные. Проверочный контур задается редакторским
  процессом, приемочной матрицей публичных маршрутов и пакетом тестовых доказательств для
  репрезентативных сущностей. \\
  \hline
  Самостоятельная сборка маркетинговых страниц из переиспользуемых блоков &
  Требование независимого редакторского цикла без ручной правки frontend-кода при типовых
  изменениях структуры страницы &
  Реализация раскрывается в подразделах главы~2 о \texttt{page}, \texttt{Dynamic Zone} и
  связке \texttt{CMS}-блоков с frontend-рендерингом. Проверка связывается с редакторским
  процессом подготовки и публикации страницы, а также с приемочными сценариями для
  репрезентативных \texttt{CMS}-страниц и их \texttt{preview}. \\
  \hline
  Поддержка \texttt{ru/en} для ядра публичной витрины и контента с маршрутами \texttt{/:locale/...} &
  Мультиязычность публичного контента, необходимость локализованных маршрутов,
  метаданных и редакторских сценариев; зафиксированная граница объема для карьерного модуля &
  Реализация раскрывается в главе~2 через локализуемую модель данных, маршрутизацию
  \texttt{/:locale/...} и \texttt{CMS}-управляемые глобальные данные. Проверка должна
  вестись по приемочной матрице локализации и пакету тестовых доказательств; при этом
  карьерный модуль трактуется как отдельный публичный контур, а не как непроверенная
  часть общего \texttt{ru/en}. \\
  \hline
  Управляемые \texttt{SEO}, \texttt{Open Graph} и \texttt{sitemap} &
  Требование поисковой видимости и редакторского управления метаданными без правки
  шаблонов витрины &
  Реализация раскрывается в главе~2 через \texttt{SEO}-модель \texttt{CMS}, единый контур
  метаданных и генерацию \texttt{sitemap}. Проверка связывается с приемочной матрицей
  \texttt{SEO}/\texttt{sitemap} и пакетом тестовых доказательств для публичных и
  \texttt{preview}-маршрутов. \\
  \hline
  Защищенный \texttt{preview mode} для чернового контента &
  Необходимость проверить материал до публикации и развести draft- и public-состояния &
  Реализация раскрывается в главе~2 через server-side \texttt{preview} на стороне
  frontend и ограничение публичного \texttt{API} опубликованным контентом. Верификация
  опирается на приемочную матрицу \texttt{preview}-сценариев, модель безопасности,
  редакторский процесс и пакет тестовых доказательств. \\
  \hline
  Публикация через схему \texttt{webhook -> rebuild} &
  Требование воспроизводимой публикации для предсобранной витрины и разделение ролей
  между \texttt{CMS} и frontend &
  Реализация раскрывается в главе~2 через подразделы о публикации и развертывании.
  Верификация связывается с описанием контура публикации и развертывания, приемочной
  матрицей публикации и пакетом тестовых доказательств по \texttt{webhook}, сборке и
  публичному результату пересборки. \\
  \hline
  Разделение ролей, прав доступа и редакторских полномочий &
  Требование независимого редакторского цикла и отделение подготовки материала от его
  публикации и системного администрирования &
  Реализация раскрывается в главе~2 через подраздел о ролях и правах доступа. Проверка
  опирается прежде всего на формальную модель безопасности, редакторский процесс и
  артефакты контура синхронизации ролей; для этого требования не следует подменять
  формальную верификацию декларативным тезисом о наличии ролей. \\
  \hline
  Защищенные публичные формы для лидов и откликов &
  Предметная область включает маркетинговую коммуникацию и карьерный сценарий, где
  публичная форма должна приводить к управляемой прикладной записи &
  Реализация раскрывается в главе~2 через подразделы о карьерном модуле и валидации
  публичных форм. Проверка связывается с приемочной матрицей форм, пакетом тестовых
  доказательств и данными прикладных записей, появляющихся после валидной отправки. \\
  \hline
\end{longtable}
\endgroup

Такая матрица показывает, что функциональные требования в рамках ВКР трактуются не как
абстрактный перечень возможностей, а как трассируемый контракт между аналитической,
проектной и проверочной частью. При этом характер верификации у требований различается:
публичные и редакторские сценарии проверяются через приемочный контур и тестовые
доказательства, тогда как ролевая модель, редакторский процесс и публикационный контур
дополнительно требуют формального описания эксплуатационных правил и границ доступа.

\subsection{Нефункциональные требования}

Нефункциональные требования в данной работе определяются не только общими качественными
ожиданиями от веб-системы, но и выбранной \texttt{CMS-first} архитектурой. Для
маркетинговой платформы принципиально, что эксплуатационные свойства должны
рассматриваться вместе с их доказуемостью: часть из них подтверждается тестами и
сборочными артефактами, а часть фиксируется как осознанная архитектурная граница,
которую нельзя подменять недоказанными количественными обещаниями.

\Needspace{12\baselineskip}
\begingroup
\footnotesize
\begin{longtable}{|L{0.22\linewidth}|L{0.22\linewidth}|L{0.46\linewidth}|}
  \caption{Трассировка нефункциональных требований к системе}
  \label{tab:nonfunctional-requirements-traceability} \\
  \hline
  \textbf{Требование} & \textbf{Источник требования} & \textbf{Контур реализации и верификации} \\
  \hline
  \endfirsthead
  \caption*{Продолжение таблицы~\ref{tab:nonfunctional-requirements-traceability}} \\
  \hline
  \textbf{Требование} & \textbf{Источник требования} & \textbf{Контур реализации и верификации} \\
  \hline
  \endhead
  \hline
  \endfoot
  Ориентация на малую или среднюю корпоративную нагрузку и публикацию через пересборку &
  Масштаб предметной области и решение не усложнять систему под высокую нагрузку и
  сценарии реального времени, не входящие в тему ВКР &
  Реализация выражается в разделении \texttt{CMS} и витрины, предварительной сборке
  frontend и отсутствии контура доставки в реальном времени. Проверка здесь носит
  характер архитектурной границы: в главе~2 и тестовом контуре фиксируются сборочные и
  браузерные приемочные показатели, но не заявляется отдельная нагрузочная валидация
  высокого масштаба. \\
  \hline
  Воспроизводимый контур развертывания и публикации &
  Требование предсказуемого развёртывания и обновления витрины без ручного запуска
  frontend-команд редактором &
  Реализация раскрывается в главе~2 через подразделы о публикации и развертывании, а также
  через раздельные \texttt{Docker}-контуры frontend и \texttt{CMS}. Верификация
  связывается с описанием контура публикации и развертывания, приемочной матрицей
  публикации, сборочными артефактами и внешним подтверждением пути
  \texttt{rebuild}. \\
  \hline
  Базовый уровень безопасности для эксплуатационного контура &
  Необходимость защищать draft-контент, публичные формы, прикладные записи и
  административный доступ без ухода в отдельную тему информационной безопасности &
  Реализация раскрывается в главе~2 через \texttt{preview}-защиту, валидацию форм,
  ограничение публичного \texttt{API} и ролевую модель. Верификация связывается с
  \texttt{security model}, приемочной матрицей \texttt{preview}/forms и пакетом тестовых
  доказательств; дополнительные подтверждения вне этого контура не заявляются. \\
  \hline
  Сопровождаемость и согласованность развития системы &
  Требование развивать \texttt{CMS} и витрину как части одной платформы без
  рассинхронизации модели данных, маршрутов и представления &
  Реализация раскрывается в главе~2 через монорепозиторий \texttt{Nx + pnpm},
  типизированный \texttt{API}-контракт, централизованную модель данных и переиспользуемые
  блоки. Верификация для этого свойства носит главным образом структурный характер и
  опирается на архитектурное описание, кодовую организацию и редакторский процесс, а не
  на отдельную числовую метрику. \\
  \hline
  Предсказуемая отдача, индексируемость и приемлемая производительность публичных страниц &
  Архитектурный акцент на предсобранной витрине и поисковой видимости маркетингового
  контента &
  Реализация раскрывается в главе~2 через \texttt{Astro}-сборку, единый \texttt{SEO}-контур
  и генерацию \texttt{sitemap}. Верификация связывается с приемочной матрицей
  \texttt{SEO}/\texttt{sitemap}, сборочными артефактами и браузерными приемочными
  измерениями; при этом корректно говорить именно о локальном приемочном уровне, а не о
  метриках \texttt{Web Vitals} в реальной эксплуатации. \\
  \hline
  Доступность публичной витрины &
  Требование корректного пользовательского взаимодействия с ключевыми маршрутами и
  формами; нормативный ориентир \texttt{WCAG 2.2}~\cite{wcag22} &
  Реализация раскрывается в главе~2 через структурированную маршрутизацию, layout,
  метаданные и формы. Верификация связывается с приемочной матрицей доступности,
  браузерным аудитом и пакетом тестовых доказательств для репрезентативных публичных
  маршрутов. \\
  \hline
\end{longtable}
\endgroup

Таким образом, нефункциональные требования в рамках данной ВКР задают не только желаемые
свойства системы, но и формат инженерной честности при их интерпретации: воспроизводимые
контуры сборки, публикации, безопасности и браузерных приемочных измерений должны
подтверждаться
фактическими артефактами, а не выводиться из общих ожиданий к современному web-стеку.

\subsection{Ограничения и нецели проекта}

Границы решения в данной работе задаются не как перечень случайно неохваченных функций,
а как осознанное сокращение класса задач до \texttt{CMS-first} платформы для
маркетингового контента. За пределы проекта вынесены прежде всего подсистемы, которые
существенно меняют характер модели данных, публикационного контура, безопасности и
эксплуатации:
\begin{itemize}
  \item функции \texttt{e-commerce}, поскольку они требуют отдельного транзакционного
  домена: каталогов, корзины, заказов, оплат и связанной бизнес-логики, не относящейся к
  редакторскому управлению маркетинговым контентом;
  \item личные кабинеты и пользовательская аутентификация на стороне витрины, так как они
  переводят систему из плоскости \texttt{CMS-first} публикации в плоскость
  пользовательской платформы с собственным контуром идентификации, сессий и авторизации;
  \item сложные многошаговые процессы согласования, поскольку в рамках ВКР сознательно
  фиксируется более компактная и проверяемая схема \texttt{draft -> preview -> publish} с
  разграничением ролей, а не отдельный движок согласования;
  \item совместное редактирование в реальном времени, так как оно требует иной модели
  конкурентного доступа, разрешения конфликтов и синхронизации состояния.
\end{itemize}

К осознанным границам относится и мультиязычный контур результата. Обязательной частью
публичного \texttt{ru/en} в данной работе выступают ядро публичной витрины, \texttt{page},
\texttt{article} и \texttt{project}, тогда как карьерный модуль сохраняется как отдельный
прикладной публичный контур с локализуемыми данными в \texttt{CMS} и локализуемым
\texttt{preview}, но без обязательного переноса публичных маршрутов в общий
\texttt{/:locale/...} шаблон. Аналогично списковые маршруты разделов могут сохранять
маршрутно-управляемый слой \texttt{SEO}, тогда как \texttt{CMS}-управляемая поверхность
метаданных осознанно ограничивается ключевыми страницами и детальными сущностями.

Принципиально, что за рамки работы при этом не выводятся \texttt{preview mode}, базовые
редакторские сценарии, защита публикации, разделение ролей и защита публичных форм.
Именно они образуют минимально достаточный инженерный контур, позволяющий считать
разрабатываемую систему не демонстрационным сайтом, а управляемой контентной платформой
для корпоративной маркетинговой витрины.

\section{Выводы по аналитической главе}

Проведенный анализ показывает, что корпоративный маркетинговый сайт следует рассматривать
как контентную систему, а не как набор редко изменяемых страниц. Для такой предметной
области существенны регулярные публикации, мультиязычность, управление метаданными,
сценарий предварительного просмотра и воспроизводимая доставка контента на публичную
витрину. Эти свойства затруднительно обеспечивать последовательно в архитектуре, где
слой управления контентом жестко связан со слоем отображения.

Сопоставление подходов к управлению контентом показывает, что \texttt{headless CMS}
лучше соответствует задачам рассматриваемой предметной области, поскольку позволяет
разделить контентную модель и frontend-представление, использовать предварительную сборку
маркетинговых страниц и выстроить публикационный контур через \texttt{API} и
\texttt{webhook -> rebuild}. В этом контексте выбор \texttt{Strapi} обоснован как выбор
\texttt{CMS}-ядра с поддержкой структурированных сущностей, локализации и редакторских
возможностей, а выбор \texttt{Astro} --- как выбор публичной витрины, ориентированной на
предсобранное представление контента и контролируемую публикацию.

На этой основе в главе сформулированы функциональные и нефункциональные требования к
системе, причем они оформлены как трассируемый набор условий: для каждого требования
определены его источник, контур проектной реализации и способ дальнейшей проверки. Тем
самым в качестве входа во вторую главу выступает не просто перечень желаемых функций, а
формализованный контракт между архитектурой, моделью данных, эксплуатационными
решениями и приемочным контуром.

Следовательно, во второй главе внимание может быть сосредоточено на проектировании
архитектуры, модели данных, редакторских сценариев, контуров публикации и развертывания,
модели безопасности и тестирования как на средствах реализации и верификации
сформированных требований. Зафиксированные ограничения при этом задают не слабые места
проекта, а допустимые границы решения, внутри которых результат остается инженерно
целостным и проверяемым.
